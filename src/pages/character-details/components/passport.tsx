import type { CharacterPreviewProps } from "../../../shared/components/character-preview";
import CharacterPreview from "../../../shared/components/character-preview";

export default function Passport(props: CharacterPreviewProps) {

    return (
        <CharacterPreview {...props} />
    )
}